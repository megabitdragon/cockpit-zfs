[Unit]
Description=FastAPI Notification Service
After=network.target

[Service]
User=root
WorkingDirectory=/opt/45drives/houston/
ExecStart=/usr/bin/python3 -m uvicorn notification_api:app --host 0.0.0.0 --port 8000 --reload
Restart=always

[Install]
WantedBy=multi-user.target
